#include <stdio.h>
#include <stdlib.h>

// Node for linked list
typedef struct {
    int vertex;
    struct Node* next; 
} Node;

typedef struct {
    int numBertices;
    Node **adjLists;
} Graph;


int main() {

    return 0;
}